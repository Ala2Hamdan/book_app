<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Book Collection</title>
  </head>
  <body>
    <main>
         <div class="book">
        <% searchResults.forEach( book => { %>
            <h3><%= book.title %></h3>
        <% }) %>
        </div>
    </main>
  </body>
</html>